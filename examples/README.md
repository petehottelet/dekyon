# Example output

This is what dekyon writes to your notes repo - real files, nothing hidden.
`sessions/<project>/` holds one note per session, a newest-first `index.md`,
and a rolling `lessons.md` ledger. Browse them here to see the format before
installing.
