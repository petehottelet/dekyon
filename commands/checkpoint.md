---
description: Save a mid-session checkpoint note to the notes repo right now
---
Invoke the dekyon skill and follow its "Writing a mid-session
checkpoint" section: compose a checkpoint note from this conversation in the
standard five-section format, write it into the notes repo from
~/.claude/dekyon.json (`repo_dir`), update that project's index.md,
then commit and push. Report the resulting file path and whether the push
succeeded. Additional focus from the user, if any: $ARGUMENTS
