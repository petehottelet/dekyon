{
  "description": "dekyon: on SessionEnd, save a narrative session note to your GitHub-synced notes repo",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python3 >/dev/null 2>&1; then exec python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/session_end.py\"; elif command -v py >/dev/null 2>&1; then exec py -3 \"${CLAUDE_PLUGIN_ROOT}/scripts/session_end.py\"; else exec python \"${CLAUDE_PLUGIN_ROOT}/scripts/session_end.py\"; fi",
            "timeout": 15,
            "statusMessage": "Saving session note (dekyon)"
          }
        ]
      }
    ]
  }
}
